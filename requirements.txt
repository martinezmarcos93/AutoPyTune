# --- núcleo: GUI + motor de audio (afinar / pulir) ---
PyQt6
sounddevice
soundfile
librosa
numpy
psola
pedalboard
scipy

# --- karaoke Etapa 1: texto exacto cantado ---
# La verdad final la transcribió Marcos a mano (data/07_karaoke/LETRAS CON VERSOS
# REPETIDOS FINAL.txt) y src/compilar_letras.py la compila a los NN-Tema.txt.
# Antes se exploró con faster-whisper (ASR) y con Gemini multimodal (escucha el
# audio y devuelve la letra cantada); ambos quedaron como pasos intermedios.
# google-genai: SDK de Gemini (Apache-2.0, auditado 2026-06-26, sin CVEs).
# truststore: usa el trust store de Windows para el SSL del antivirus (descargas HF).
faster-whisper
google-genai
truststore

# --- karaoke Etapa 2: timing por palabra (forced alignment) ---
# torchaudio trae el modelo MMS_FA (CTC forced alignment, multilingüe). Alinea el
# texto exacto contra la voz IA y da el tiempo de cada palabra (src/alinear_forzado.py).
torchaudio

# --- separación de voz / instrumental (Demucs) ---
demucs
torch
# Con GPU NVIDIA, instalar el build CUDA de torch para acelerar:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# --- opcional: mejor calidad de time-stretch ---
# pyrubberband requiere el binario externo "Rubber Band". Sin él se usa
# librosa automáticamente. Descomentar solo si instalás ese binario.
# pyrubberband

# --- opcional: descargar audio desde YouTube para analizarlo ---
# imageio-ffmpeg trae un binario de ffmpeg incluido (no hace falta instalarlo aparte).
# NOTA: YouTube puede bloquear la descarga (SABR/PO-Token); más fiable usar archivos locales.
yt-dlp
imageio-ffmpeg
